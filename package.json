{
  "name": "devotedhealth",
  "version": "1.0.0",
  "description": "cli tool to perform basic CRUD operations",
  "main": "bin index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "reset": "npm uninstall -g . && npm install -g ."
  },
  "bin": {
    "dhealth": "./bin/index.js"
  },
  "author": "Zach",
  "license": "ISC",
  "dependencies": {
    "lokijs": "^1.5.12",
    "todocli-frombd": "^1.0.1",
    "yargs": "^13.2.4"
  }
}
